{
  "name": "@example/my-lib",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "nx run-many -t build --skip-nx-cache"
  },
  "private": true,
  "devDependencies": {
    "nx": "16.6.0",
    "typescript": "^5.1.6"
  },
  "workspaces": [
    "packages/*"
  ]
}
